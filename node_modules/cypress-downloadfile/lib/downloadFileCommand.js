"use strict";

Cypress.Commands.add('downloadFile', function (url, dir, fileName) {
  return cy.getCookies().then(function (cookies) {
    return cy.task('downloadFile', {
      url: url,
      directory: dir,
      cookies: cookies,
      fileName: fileName
    });
  });
});